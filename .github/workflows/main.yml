name: Distirbuted workflow tasks

on: push

jobs:
  constructor:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo ls
          find ./android/ -name 'action.yml' -exec cp "{}" ./github/workflows  \;

  android_ci_job:
    needs: constructor
    uses: action.yml@master
