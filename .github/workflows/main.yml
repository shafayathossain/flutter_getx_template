name: Distirbuted workflow tasks

on: push

jobs:
  copy_android_workflow:
    uses: shafayathossain/flutter_getx_template/.github/workflows/action.yml@master
    secrets:
      FIREBASE_APP_DISTRIBUTION_KEY_BASE_64: ${{ secrets.FIREBASE_APP_DISTRIBUTION_KEY_BASE_64 }}