name: Distirbuted workflow tasks

on: push

jobs:
  android_ci_job:
    name: Start Android CI workflow
    uses: ${{github.repository_owner}}/flutter_getx_template/android/.github/workflows/action.yml@$main
