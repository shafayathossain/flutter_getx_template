name: Distirbuted workflow tasks

on: push

jobs:

  android_ci_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: My first step
        uses: ./android/.github/workflow
